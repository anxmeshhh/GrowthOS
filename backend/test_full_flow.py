import requests
import json
import time

API_BASE = "https://api.growth-os.tech/api"

def test_flow():
    print("Testing flow...")
    # I don't have a Google access token, so I can't test /auth/google/.
    # But I can test /auth/login/ using a test user if I create one!
    pass

if __name__ == "__main__":
    test_flow()
